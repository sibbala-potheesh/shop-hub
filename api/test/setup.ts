import 'reflect-metadata';
process.env.NODE_ENV = process.env.NODE_ENV || 'test';

// if you use dotenv in tests, optionally load it:
// import dotenv from 'dotenv';
// dotenv.config({ path: path.resolve(__dirname, '../.env.test') });
